<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Posts</title>
  </head>
  <body>
    <h1>All Posts</h1>

    <% if (posts.length > 0) { %> <% posts.forEach(post => { %>
    <div>
      <h2><%= post.title %></h2>
      <p><%= post.content %></p>
      <p>Author: <%= post.author %></p>
      <p id="time"><%= new Date(post.created_at).toLocaleString() %></p>
      <hr />
    </div>
    <% }); %> <% } else { %>
    <p>No posts found!</p>
    <% } %>
  </body>
</html>
